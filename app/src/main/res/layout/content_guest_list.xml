<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:id="@+id/scanning_events_layout"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:layout_behavior="@string/appbar_scrolling_view_behavior"
        tools:showIn="@layout/activity_guest_list"
        android:orientation="vertical"
        tools:context=".activity.GuestListActivity">

    <TextView
            android:id="@+id/guest_list_label"
            android:layout_width="match_parent"
            android:layout_height="50dp"
            android:text="@string/guest_list"
            android:background="@color/colorWhite"
            android:textColor="@android:color/darker_gray"
            android:textSize="24sp"
            android:paddingStart="10dp"
            android:paddingEnd="10dp"
            android:textStyle="bold"
            android:gravity="center_vertical"/>

    <EditText
            android:id="@+id/search_guest"
            android:layout_width="match_parent"
            android:layout_height="50dp"
            android:paddingStart="10dp"
            android:paddingEnd="10dp"
            android:layout_marginStart="10dp"
            android:layout_marginEnd="10dp"
            android:layout_marginBottom="20dp"
            android:hint="@string/search_for_guests"
            android:background="@drawable/search_button_background"/>

    <android.support.v7.widget.RecyclerView
            android:id="@+id/guest_list_view"
            xmlns:android="http://schemas.android.com/apk/res/android"
            android:layout_width="match_parent"
            android:layout_height="match_parent">
        <requestFocus />
    </android.support.v7.widget.RecyclerView>
</LinearLayout>
